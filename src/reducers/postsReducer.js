const defaultState = {
  posts: [],
  selectedCategory: "",
  filterTerm: ""
}

function postsReducer (state = defaultState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default postsReducer
